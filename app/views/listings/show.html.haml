%ol.breadcrumb
	%li= link_to "Rubylist", root_path
	%li= link_to @sub.category.name, @sub.category
	%li= @sub.name

%div.page-header
	%h1
		= @listing.title
		%small
			Posted 
			= time_ago_in_words(@listing.created_at)
			ago
%p= @listing.description